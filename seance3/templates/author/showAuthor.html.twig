<h1>details auteur</h1>

<img src="{{ author.picture }}" alt="{{ author.username }}" width="150">
{{ author.username }} - {{ author.email }} - {{ author.nb_books }} livres

<a href="{{ path('app_author_list') }}">Retour</a>
